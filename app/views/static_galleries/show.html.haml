.image-container
  - @gallery.images[-100..-1].each do |image|
    = image_tag image.url, class: ['image'] << ('vertical' if image.vertical?) , style: "height: #{0.8 * (33.3333 * image.height / image.width).round}vw"
